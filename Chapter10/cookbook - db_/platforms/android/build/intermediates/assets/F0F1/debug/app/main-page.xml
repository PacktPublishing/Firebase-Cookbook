<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo" class="page">
    <Page.actionBar>
        <ActionBar title="Firebase CookBook" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>
    <!-- <StackLayout class="p-20">
        <TextField text="{{ newIdea }}" hint="Add here your shiny idea"/>
        <Button text="Add new idea to my bucket" tap="{{ onTap }}" class="btn btn-primary btn-active"/>
        <ListView items="{{ ideas }}">
            <ListView.itemTemplate>
                <Label text="{{ $value }}" />
            </ListView.itemTemplate>
        </ListView>
    </StackLayout> -->
    <StackLayout>
        <Label text="Login Page" textWrap="true" style="font-weight: bold; font-size: 18px; 
text-align: center; padding:20"/>
        <TextField hint="Email" text="{{ user_email }}" />
        <TextField hint="Password" text="{{ user_password }}" secure="true"/>
        <Button text="LOGIN" tap="{{ passLogin }}" class="btn btn-primary" /> 
        <StackLayout class="hr-dark"></StackLayout>
        <Button text="Anonymous login" tap="{{ anonLogin }}" class="btn"/> 
        <Button text="Google Sign-in" tap="{{ passLogin }}" class="btn" style="color:red"/> 
    </StackLayout>
</Page>
